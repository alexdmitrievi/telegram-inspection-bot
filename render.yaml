services:
  - type: worker
    name: telegram-inspection-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    plan: free
    envVars: []  # Удаляем токен отсюда — переменная будет задана вручную на Render


